# Free Square Images

Add your own reusable centre-square images in this folder, then add them to `manifest.json`.

Example:

```json
{
  "id": "heart",
  "label": "Heart",
  "src": "heart.png"
}
```

Use simple lowercase IDs with hyphens. Images can be SVG, PNG, JPG, or WebP.
